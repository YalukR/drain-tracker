import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.drenajes.app',
  appName: 'Drenajes',
  webDir: 'dist/drain-tracker/browser'
};

export default config;
